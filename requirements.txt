# Runtime dependencies. For development extras use: pip install -e ".[dev]"
#
# Floors are minimums a resolver may legitimately pick, not decoration: the
# previous ones admitted a starlette, PyJWT, cryptography and python-multipart
# with published CVEs. Each floor below is the lowest release pip-audit reports
# clean; `pip-audit` runs in CI so this stays true.
fastapi>=0.141.1
uvicorn[standard]>=0.30
pydantic>=2.13
pydantic-settings>=2.15
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13
asyncpg>=0.29
aiosqlite>=0.20
redis>=5.0
celery>=5.4
httpx>=0.27
cryptography>=50.0.0
argon2-cffi>=23.1
pyjwt>=2.13.0
structlog>=24.2
networkx>=3.3
feedparser>=6.0
python-multipart>=0.0.31
email-validator>=2.1
